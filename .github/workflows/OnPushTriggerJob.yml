name: Push Trigger job

on:
  push:
  path-ignore:
    - '.github/**'

jobs:
  get-github-context:
    runs-on: windows-latest

    steps:
      - name: Print GitHub context
        run: echo '${{ toJSON(github) }}'
